---
human_revised: false
plan: maintenance-go-agent-names
task: T1
depends-on: []
concerns: [bridge]
files: [go/internal/session/store.go, go/internal/session/store_test.go]
status: pending
targets: [cli]
aux: []
summary: Merge the live `claude agents --json` names into the Go Claude session store, custom-title first, degrading silently when the binary fails.
---

# T1 — Merge live agent names into the Claude store

## What to do

In `claudeStore`, call `ClaudeAgents(home)` (error ignored), fill an empty
transcript title with the live name for that id, and append live agents
that have no transcript. Covers AC1-AC6.

## Context

`Resolver.Slug` already implements id -> name and ambiguity for any store;
only the Claude store's data is incomplete.

## Implementation

TDD with the `fakeClaude` helper (`agents_test.go`) on a temp PATH; the
missing-binary case uses a PATH of an empty dir and a home without
`.local/bin/claude`.

## Done when

- [ ] New store/resolver tests fail first, then pass.
- [ ] `go test -count=1 ./...`, `go vet ./...`, `gofmt -l .` clean in `go/`.
