---
human_revised: false
plan: maintenance-go-agent-names
task: T2
depends-on: [T1]
concerns: [bridge]
files: []
status: done
targets: [cli]
aux: []
summary: Rebuild `bin/alter-bridge` and prove in scratch roots that bash and Go `watchpaths` register the same mailbox for a live unrenamed Claude session, and Go `hook claude` drains it.
---

# T2 — Scratch parity evidence

## What to do

Run `go/build.sh`. Pick a live unrenamed session id from `bin/alter-bridge
who` (read-only). Feed one SessionStart payload to bash `watchpaths` and Go
`watchpaths claude`, each with its own scratch `ALTER_BRIDGE_ROOT`; compare
the registered mailbox. Drop a message into the Go scratch mailbox with
`send --from` (scratch root) and check `hook claude` drains it. Covers AC7-AC8.

## Context

Reproduces the `maintenance-go-cutover` Blocker comparison.

## Implementation

Scratch only; never the real `~/.alter-bridge`, no real session messaged.

## Done when

- [ ] Both registrations name the same `claude/<auto-name>` directory.
- [ ] `hook claude` prints the scratch message and archives it.
