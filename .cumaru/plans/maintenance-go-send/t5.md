---
human_revised: false
plan: maintenance-go-send
task: T5
depends-on: [T2, T4]
concerns: [bridge]
files: [go/cmd/alter-bridge/send.go, go/internal/nudge/nudge.go, go/internal/nudge/nudge_test.go]
status: done
targets: [cli]
aux: []
summary: Nudge a live Codex recipient via codex queue after delivery, silently skipping on any failure.
---

# T5 — Codex nudge

## What to do

After a successful delivery to a `codex:` recipient, run:

```
codex queue --thread <thread-id> --message "alter-bridge: new message from <from> (msgid <id>). Run: <bridge> inbox <to>"
```

- `<thread-id>` from `session.CodexThreadForSlug`; fall back to the slug itself (`codex queue --thread` also accepts the session name).
- `codex` absent, lookup empty, or non-zero exit → ignore; stdout/stderr discarded; `send` still exits 0.

## Context

Bash reference: `alter-bridge:213-222`. The nudge is for immediacy only; the message is already delivered. Keep the `Run:` path pointing at the bash script until cutover.

## Implementation

1. Inject the command runner; tests first: codex recipient triggers exactly one call with the expected args; non-codex recipient triggers none; runner error does not change exit status.
2. Implement and call from `send`.

## Done when

- [ ] `go test ./...` passes.
- [ ] Manual check: sending to a live Codex session surfaces the queued notice there.
