---
human_revised: false
plan: maintenance-go-watchpaths
task: T1
depends-on: []
concerns: [bridge]
files: [go/cmd/alter-bridge/hookinput.go, go/cmd/alter-bridge/hook.go]
status: pending
targets: [cli]
aux: []
summary: Lift hook's argument parsing, payload decoding, and ~/agentic-workspace gate out of runHook into shared helpers, with hook's tests unchanged and green.
---

# T1 — Extract shared hook argument, payload, and workspace-gate helpers

## What to do

- New `go/cmd/alter-bridge/hookinput.go` holding what `hook` and
  `watchpaths` share:
  - `hookAddress(arg) (address.Address, error)` — a bare known provider
    (optional `#`, empty value) or `address.Parse` for `provider:value`.
  - `hookSession(stdin, home, wd) (sid string, inWorkspace bool)` — decode
    the payload, pick the scope dir (`cwd` when an existing directory, joined
    onto `wd` when relative, else `wd`), report whether it is
    `home/agentic-workspace` or below, and return the first non-empty
    `session_id` / `thread_id` / `threadId` string.
  - `firstString` moves here.
- `hook.go`: `runHook` calls the helpers; usage handling, resolve, and drain
  stay put.

## Context

Behavior-preserving refactor (DRY, second caller arrives in T2). The safety
net is the existing `hook_test.go`, which must pass unmodified.

## Implementation

1. Run `go test -count=1 ./cmd/alter-bridge/` green before the move.
2. Extract; rerun the same tests, `go vet`, `gofmt -l`.

## Done when

- [ ] `hook_test.go` unmodified and green; `go test -count=1 ./...`, `go vet ./...` clean; `gofmt -l .` empty.
