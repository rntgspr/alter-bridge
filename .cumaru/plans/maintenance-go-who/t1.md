---
human_revised: false
plan: maintenance-go-who
task: T1
depends-on: []
concerns: [bridge]
files: [go/internal/session/agents.go, go/internal/session/agents_test.go]
status: done
targets: [cli]
aux: []
summary: Add session.ClaudeAgents, the live `claude agents --json` reader (interactive only, PATH then ~/.local/bin fallback), tested against a fake binary.
---

# T1 — `session.ClaudeAgents`

## What to do

- Add `session.Agent{ID, Name, Cwd}` and `session.ClaudeAgents(home string) ([]Agent, error)`.
- Resolve the binary with `exec.LookPath("claude")`, else `<home>/.local/bin/claude` (bash `claude_bin()`).
- Run `claude agents --json`, decode the array, keep `kind == "interactive"` entries in CLI order; return an error when the binary is missing, exits non-zero, or prints invalid JSON.

## Context

No Go reader of `claude agents --json` exists yet: the Claude `Store` reads transcripts (all titled sessions, no `cwd`, no liveness). The `who` Claude half needs live sessions and their `cwd`, so this is a separate reader, not a second copy of the store.

## Implementation

1. Tests first (`agents_test.go`): a fake `claude` shell script in a temp dir on `PATH` prints a fixture mixing `interactive` and `background` kinds; interactive rows come back in order with id/name/cwd. Fallback: `PATH` without `claude`, fake at `<home>/.local/bin/claude` → same rows. Missing binary, non-zero exit, and invalid JSON → error.
2. Watch them fail (undefined `ClaudeAgents`), then implement.

## Done when

- [x] `go test -count=1 ./...` passes, each new test seen failing first; `go vet ./...` clean; `gofmt -l .` empty.
