---
human_revised: false
plan: maintenance-plugin-standard
task: T2
depends-on: [T1]
concerns: [bridge]
files: [README.md, skills/alter-bridge/SKILL.md, skills/alter-bridge/scripts/install-hooks.sh]
status: pending
targets: [plugin, skill]
aux: []
summary: Rewrite the README install and layout sections and the SKILL.md setup note for the plugin flow, and remove `install-hooks.sh`.
---

# T2 — Docs and installer retirement

## What to do

- README: Install via `./install.sh` (build + both CLIs), Codex `/hooks`
  trust, `/reload-plugins` for running Claude sessions; Layout lists the new
  files; drop the stale `plugins/.claude-plugin/...` and cache claims.
- SKILL.md: setup points at `install.sh`; note the binary is on the Claude
  Bash `PATH` while the plugin is enabled, the absolute path works everywhere.
- Delete `skills/alter-bridge/scripts/install-hooks.sh`: it writes the very
  global hooks the plugin now provides, and running it would double them.

## Done when

- [ ] `rg install-hooks README.md skills` finds nothing.
