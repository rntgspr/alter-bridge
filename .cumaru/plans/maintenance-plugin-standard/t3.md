---
human_revised: false
plan: maintenance-plugin-standard
task: T3
depends-on: [T2]
concerns: [bridge]
files: []
status: blocked
targets: [plugin, hooks]
aux: []
summary: Cut this machine over from the manual hooks and the old agent-bridge plugin to the alter-bridge plugin on both runtimes, with backups and without a delivery gap or double wiring.
---

# T3 — Live cutover of this machine

## What to do

Global files only (`~/.claude/settings.json`, `~/.codex/hooks.json`,
`~/.codex/config.toml`, plugin registries), each backed up to
`.bak.<timestamp>` first:

1. Verify the release-backed Claude plugin and the Codex plugin installed by
   `install-codex.sh`, including the trusted Codex hook.
2. Remove the three manual alter-bridge hooks from `~/.claude/settings.json`.
3. Uninstall `agent-bridge@agents-marketplace` and remove the
   `agents-marketplace` registration on both CLIs (it lists nothing else).
4. Codex: confirm the plugin hook is trusted in `/hooks`; only then remove any
   remaining manual entry from `~/.codex/hooks.json`.

## Context

The ordering constraint is recorded in the plan's Risks: steps 2 and 4 need
Renato's `/reload-plugins` in live Claude sessions and `/hooks` trust in
Codex, so this task runs only on his go-ahead.

## Done when

- [ ] AC7 and AC8 evidence recorded.

## Observed state (2026-09-25)

- Claude has `alter-bridge@alter-bridge` 0.2.0 enabled at both project and
  user scope, while its three manual settings hooks remain. AC7's
  exactly-one-entry condition is not established.
- Codex lists `alter-bridge@alter-bridge-codex` 0.2.0 installed and enabled;
  its bundled `UserPromptSubmit` hook has a trusted hash in `config.toml`.
  This release-based Codex path differs from the local checkout install
  specified by this original task. No full AC7 or AC8 verdict is recorded.
