---
human_revised: false
plan: maintenance-rename-alter-bridge
task: T3
depends-on: [T2]
concerns: [bridge]
files: [skills/alter-bridge/scripts/alter-bridge, skills/alter-bridge/scripts/install-hooks.sh, skills/alter-bridge/SKILL.md]
status: done
targets: [cli, hooks]
aux: []
summary: Framework guidance for T3 — rename env var and default mailbox root and its required workflow.
---

# T3 — Rename env var and default mailbox root

## What to do

Rename the `AGENT_BRIDGE_ROOT` override to `ALTER_BRIDGE_ROOT` and the
default mailbox root from `~/.agent-bridge` to `~/.alter-bridge` everywhere
the CLI, the hook installer, and the skill doc reference them.

## Context

`skills/alter-bridge/scripts/alter-bridge:9` — `ROOT="${AGENT_BRIDGE_ROOT:-$HOME/.agent-bridge}"`.
`install-hooks.sh:38-40` resolves `bridge="$here/agent-bridge"` — after T2's
rename this must read `$here/alter-bridge`. `install-hooks.sh:194` seeds the
Codex hook description string ("Inject pending agent-bridge messages...").
`SKILL.md` documents `AGENT_BRIDGE_ROOT` and `~/.agent-bridge` in its
addressing/archive sections (left untouched by T2).

## Implementation

1. In `skills/alter-bridge/scripts/alter-bridge`: `ROOT="${AGENT_BRIDGE_ROOT:-$HOME/.agent-bridge}"` -> `ROOT="${ALTER_BRIDGE_ROOT:-$HOME/.alter-bridge}"`; update the `relay.log` path comment/usage if it embeds `AGENT_BRIDGE_ROOT`.
2. In `skills/alter-bridge/scripts/install-hooks.sh`: `bridge="$here/agent-bridge"` -> `bridge="$here/alter-bridge"`; update the error message string; update the Codex hook `description` seed text.
3. In `skills/alter-bridge/SKILL.md`: replace remaining `AGENT_BRIDGE_ROOT` / `~/.agent-bridge` occurrences with `ALTER_BRIDGE_ROOT` / `~/.alter-bridge`.
4. Do not migrate the actual `~/.agent-bridge` directory on disk here — that is T5, after every code reference is consistent.

## Done when

- [ ] `grep -rn 'AGENT_BRIDGE\|agent-bridge' skills/alter-bridge/` returns nothing.
- [ ] `bash skills/alter-bridge/scripts/alter-bridge who` runs without error against the still-live `~/.agent-bridge` when `ALTER_BRIDGE_ROOT=$HOME/.agent-bridge` is exported (sanity check the rename didn't break parsing, ahead of T5's migration).
