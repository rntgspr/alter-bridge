---
human_revised: false
plan: maintenance-rename-alter-bridge
task: T4
depends-on: [T2, T3]
concerns: [bridge]
files: [README.md]
status: done
targets: [cli, plugin]
aux: []
summary: Framework guidance for T4 — update README.md references and its required workflow.
---

# T4 — Update README.md references

## What to do

Replace every `agent-bridge` / `AGENT_BRIDGE_ROOT` / `~/.agent-bridge`
occurrence in `README.md` with the `alter-bridge` equivalents, matching
what T2/T3 landed in code.

## Context

`README.md` title, marketplace install commands
(`claude plugin add agent-bridge@agents-marketplace`, `codex plugin add
agent-bridge@agents-marketplace`), the plugin cache paths
(`~/.claude/plugins/cache/agents-marketplace/agent-bridge/`,
`~/.codex/plugins/cache/agents-marketplace/agent-bridge/`), the
`AGENT_BRIDGE_ROOT` env var doc, and the file-layout table all reference the
old name.

## Implementation

1. Title: `# agent-bridge` -> `# alter-bridge`.
2. Marketplace commands: `agent-bridge@agents-marketplace` -> `alter-bridge@agents-marketplace` (both `claude plugin add` and `codex plugin add` lines).
3. Cache paths: `.../agents-marketplace/agent-bridge/` -> `.../agents-marketplace/alter-bridge/` (both runtimes).
4. `skills/agent-bridge/...` path references -> `skills/alter-bridge/...`.
5. `AGENT_BRIDGE_ROOT` -> `ALTER_BRIDGE_ROOT`; default `~/.agent-bridge` -> `~/.alter-bridge`.
6. File-layout table entries for `SKILL.md`, the script, and `install-hooks.sh` -> new paths.

## Done when

- [ ] `grep -n 'agent-bridge\|AGENT_BRIDGE' README.md` returns nothing.
- [ ] README's marketplace add commands and cache paths read `alter-bridge` throughout.
