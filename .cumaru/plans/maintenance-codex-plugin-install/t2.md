---
human_revised: false
plan: maintenance-codex-plugin-install
task: T2
depends-on: [T1]
concerns: [bridge]
files: []
status: pending
targets: [plugin, hooks]
aux: []
summary: Install and trust the Codex plugin, retire only legacy Codex wiring, and prove single-hook message delivery.
---

# T2 - Activate and verify Codex

## What to do

Install `alter-bridge@alter-bridge` from this checkout in Codex, trust its
prompt hook, remove the old Codex-only plugin and manual alter-bridge hook,
then verify that exactly one effective hook delivers messages.

## Context

Codex must trust a plugin hook in `/hooks` before it executes. Preserve the
manual prompt hook until trust and plugin execution are confirmed. The Claude
thread owns all Claude settings and registrations.

## Implementation

1. Add the local marketplace and install or reinstall the plugin with the
   current Codex CLI; confirm plugin and skill discovery.
2. Inspect the bundled hook and complete Codex `/hooks` trust for its exact
   definition. Confirm the trusted plugin hook runs before removing manual
   delivery.
3. Remove only the legacy Codex `agent-bridge` registration and manual
   alter-bridge hook, preserving unrelated settings and plugins.
4. Verify one effective prompt hook, scratch-root drain and archive behavior,
   and read-only `who`. Record commands, output, and any user action in the
   handoff.
5. Reconcile the Codex portion of `maintenance-plugin-standard` T3 without
   marking its Claude portion complete.

## Done when

- [ ] AC1 through AC6 have recorded PASS evidence.
- [ ] Codex has one effective alter-bridge prompt hook and no legacy wiring.
- [ ] The old joint plan reflects the completed Codex portion only.
