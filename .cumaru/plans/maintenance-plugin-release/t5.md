---
human_revised: false
plan: maintenance-plugin-release
task: T5
depends-on: [T3]
concerns: [bridge]
files: [skills/alter-bridge/scripts/alter-bridge, go/internal/nudge/nudge.go, go/internal/nudge/nudge_test.go, go/internal/address/address_test.go, README.md, skills/alter-bridge/SKILL.md]
status: done
targets: [plugin]
aux: []
summary: Remove the unwired bash script from the skill folder so the plugin ships only SKILL.md there, and repoint the Codex nudge notice at the Go binary.
---

# T5 — Remove the bash script from the skill

## What to do

- Delete `skills/alter-bridge/scripts/`, which held only the unwired bash
  fallback. The skill folder keeps just `SKILL.md`, which is what the release
  zip ships.
- Point `nudge.BridgeScript` (the `codex queue` notice text) at
  `~/agentic-workspace/papa/alter-bridge/bin/alter-bridge` instead of the
  deleted script.
- Remove the fallback mentions from `README.md` and `SKILL.md`.

## Context

Renato asked for the removal on 2026-09-24. The Go CLI has been live since
`maintenance-go-cutover`, and the bash source remains in git history.

## Implementation

1. Pin the expected notice text in `nudge_test.go` and watch it fail.
2. Make the change and turn it green.

## Done when

- [x] `go test ./...` green; the nudge test failed first on the old path.
- [x] `rg 'scripts/alter-bridge'` outside `.cumaru/` finds only the
  history-pointer comment in `address_test.go`.
