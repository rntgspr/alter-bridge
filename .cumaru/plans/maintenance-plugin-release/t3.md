---
human_revised: false
plan: maintenance-plugin-release
task: T3
depends-on: [T1]
concerns: [bridge]
files: [hooks/hooks.json, .claude-plugin/marketplace.json, README.md, skills/alter-bridge/SKILL.md, install.sh]
status: done
targets: [plugin, hooks]
aux: []
summary: Point Claude hooks at the go/ launcher, switch the marketplace entry to an archive source, and document the standard install and --plugin-dir dev loop.
---

# T3 — Hooks, marketplace `archive` source, docs

## What to do

- `hooks/hooks.json`: call `"${CLAUDE_PLUGIN_ROOT}/go/alter-bridge" <sub> claude`.
- `.claude-plugin/marketplace.json`: make `alter-bridge` an `archive` source,
  with the `url` and `sha256` placeholders that T2's workflow fills in.
- `README.md` / `SKILL.md`:
  - Claude install is `/plugin marketplace add rntgspr/alter-bridge` and
    `/plugin install alter-bridge@alter-bridge`.
  - Local dev is `claude --plugin-dir .`.
- `install.sh`: remove the Claude branch and keep the Codex branch unchanged
  (AC7).

## Context

- Leave the Codex files untouched: `hooks/codex.json`,
  `.codex-plugin/plugin.json`, `bin/`.

## Implementation

- With `--plugin-dir .`, the launcher must also work from a checkout. After
  running `go/release.sh`, verify it locally.

## Done when

- [ ] `claude --plugin-dir .` with release binaries built: the three hooks run
  against a scratch `ALTER_BRIDGE_ROOT` (watchpaths JSON, hook drains, relay
  ignores `change`).
- [ ] `rg 'install.sh' README.md skills` shows no Claude install step.
- [ ] `cumaru doctor` clean.
