---
human_revised: false
plan: maintenance-plugin-release
task: T4
depends-on: [T2, T3]
concerns: [bridge]
files: []
status: in-progress
targets: [plugin, hooks]
aux: []
summary: Renato-gated first tag/release, then install the Claude plugin via the standard GitHub marketplace flow and remove the manual Claude hooks without overlap.
---

# T4 — First release and live Claude cutover (Renato-gated)

## What to do

1. With Renato's go-ahead, push the first `v*` tag, confirm the workflow run,
   the release zip, and the `marketplace.json` commit.
2. On this machine:
   - Back up `~/.claude/settings.json`, `~/.claude/plugins/installed_plugins.json`,
     and `~/.claude/plugins/known_marketplaces.json` as `.bak.<ts>`.
   - `claude plugin marketplace add rntgspr/alter-bridge`, then
     `claude plugin install alter-bridge@alter-bridge`.
   - Remove the 3 manual Claude hooks.
   - Renato runs `/reload-plugins` in live sessions.
3. Reconcile `maintenance-plugin-standard` T3 (Claude half done here).

## Context

- Pushing a tag and publishing a release are outward-facing. Never do either
  without Renato's explicit OK.
- Leave the Codex manual hook as it is.

## Implementation

- Check for macOS quarantine on the downloaded binary. If it blocks, record
  the fix and the evidence here.

## Done when

- [ ] `claude plugin details alter-bridge` shows the skill and 3 hooks,
  sourced from the archive.
- [ ] Exactly one alter-bridge entry per Claude hook event.
- [ ] A live self-send wakes a session and drains it.

## Observed state (2026-09-25)

- GitHub published `v0.1.0` and `v0.2.0`. The latter contains separate
  `alter-bridge-v0.2.0.zip` and `alter-bridge-codex.zip` assets.
- `.claude-plugin/marketplace.json` points to the Claude `v0.2.0` zip with
  SHA-256 `cba585ba1f3ca6fed3828cf0f2cbd544a201a1a9fa853599ef20c3ccf8e9a99b`,
  matching the GitHub release asset digest. AC3 and AC4's release outcome are
  observed; the release workflow's individual steps were not independently
  audited here.
- `claude plugin details alter-bridge` lists version 0.2.0, one skill, and
  three hooks. `claude plugin list` shows enabled copies at project and user
  scope. `~/.claude/settings.json` still contains manual `SessionStart`,
  `UserPromptSubmit`, and `FileChanged` commands. The single-hook cutover and
  live self-send remain unverified; do not close T4 on this evidence.
