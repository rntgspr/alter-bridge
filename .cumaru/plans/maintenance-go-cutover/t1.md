---
human_revised: false
plan: maintenance-go-cutover
task: T1
depends-on: []
concerns: [bridge]
files: []
status: blocked
targets: [cli]
aux: []
summary: Gate the cutover on identity continuity — every live session's Go-resolved mailbox must equal the one its `who` address points at; blocked, auto-named Claude sessions resolve to their id.
---

# T1 — Identity continuity gate

## What to do

For each live session `bin/alter-bridge who` lists inside `~/agentic-workspace`,
feed the same SessionStart payload to bash `watchpaths` and Go
`watchpaths claude` against scratch roots and compare the registered mailbox
with the directory names under `~/.alter-bridge/claude/`. Read-only toward the
live root.

## Context

The Go identity model (spec Decisions, `maintenance-go-send`) keys an unnamed
session by its id; bash used the `claude agents --json` name.

## Implementation

Scratch comparison only; no code change. Outcome recorded in the plan's
`## Blocker` section.

## Done when

- [ ] Every live session resolves to the same mailbox under bash and Go, or
      Renato has chosen an option from the plan's Blocker section.
