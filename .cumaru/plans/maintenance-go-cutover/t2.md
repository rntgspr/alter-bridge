---
human_revised: false
plan: maintenance-go-cutover
task: T2
depends-on: [T1]
concerns: [bridge]
files: [skills/alter-bridge/SKILL.md, README.md, skills/alter-bridge/scripts/install-hooks.sh]
status: pending
targets: [cli, hooks, skill]
aux: []
summary: Rebuild bin/alter-bridge and move SKILL.md, README.md and install-hooks.sh to the Go binary with provider arguments, keeping the bash script as fallback.
---

# T2 — Repo cutover

## What to do

- Run `go/build.sh`; `bin/` stays gitignored.
- `SKILL.md` / `README.md`: canonical command
  `~/agentic-workspace/papa/alter-bridge/bin/alter-bridge`; invocations per
  each subcommand's `-h` (`send ... --from "claude:$CLAUDE_CODE_SESSION_ID"`,
  `inbox` / `peek` with an address, hook entry points with a provider);
  bash script named as fallback.
- `install-hooks.sh`: point at `bin/alter-bridge` (refuse when missing),
  emit `"<bin>" <sub> <provider>` without the `bash` prefix, and match
  existing entries of both the bash form and the Go form by subcommand so
  they are reconciled in place.

## Context

AC2, AC3. The live files are T3.

## Implementation

See What to do; verify the installer against a scratch `HOME` seeded with
copies of the live files (bash form -> FIXED, Go form -> untouched).

## Done when

- [ ] `go test -count=1 ./...` passes and `bin/alter-bridge` is fresh.
- [ ] Scratch-HOME installer runs show FIXED then "left untouched".
