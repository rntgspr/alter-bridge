---
human_revised: false
plan: maintenance-go-cutover
task: T3
depends-on: [T2]
concerns: [bridge]
files: []
status: done
targets: [hooks]
aux: []
summary: Rewire the live Claude and Codex hooks to the Go binary with provider arguments, backing each file up first, and verify the wired commands against scratch roots.
---

# T3 — Live rewiring

## What to do

Copy `~/.claude/settings.json` and `~/.codex/hooks.json` to
`<file>.bak.<timestamp>`, then change only the alter-bridge command strings:
`"$HOME/agentic-workspace/papa/alter-bridge/bin/alter-bridge" watchpaths claude`,
`... hook claude`, `... relay claude` (matcher and timeouts kept), and
`... hook codex`. Validate both with `jq empty`.

## Context

AC4, AC5, AC7. Files are outside the repository, so `files:` is empty.

## Implementation

Pipe SessionStart, UserPromptSubmit and FileChanged `change` payloads into
the exact wired strings with `ALTER_BRIDGE_ROOT` on a scratch copy; run
`who` and `peek` read-only against the live root.

## Done when

- [x] Backups exist, both files parse, diffs touch only command strings.
- [x] AC5 and AC7 evidence recorded in the handoff.
